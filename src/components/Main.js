import React, { useState } from "react";

const Main = () => {
  const [cardArray, setCardArray] = useState([]);

  return (
    <div className="Main">
      <p>Show clickable cards here</p>
    </div>
  );
};

export default Main;
